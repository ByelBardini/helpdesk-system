Use o agente `backend` para implementar ou modificar features no backend do helpdesk.

Escopo: pasta `server/` — Express 5, Sequelize 6, MySQL 8, Socket.IO, JWT, Bcrypt, Multer, node-cron, ExcelJS.

Descreva o que precisa ser implementado ou alterado:
$ARGUMENTS
