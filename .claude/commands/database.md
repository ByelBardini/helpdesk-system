Use o agente `database` para alterações de schema, novas migrations SQL ou mudanças nos modelos Sequelize.

Escopo: `migration/` e `server/models/` — MySQL 8, Sequelize 6.

Descreva a mudança de schema necessária:
$ARGUMENTS
